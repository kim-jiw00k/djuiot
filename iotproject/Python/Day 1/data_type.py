value_a : int = 1  # 명확한 표현 방식  파스칼 노테이션(pascal notation)
value_b : float = 1.0 # 실수형 으로 바뀜
value_c : str = 'H' # 문자열
value_d : bool = False # boolean 타입
#value_c : float = 20.0 # 재정의 가 아닌 앞서 있던 문자열 의 H가 사라 지고 20.0의 실수형 으로 바뀐다.


print(type(value_a))        # 자료형 의 형식을 알려 주는 함수는 type() 함수 이다.
print(type(value_b))
print(type(value_c))
print(type(value_d))

print("\"안녕하세요\" 라고 말했습니다.")